{
  "name": "gelbooru-viewer",
  "license": "MIT",
  "scripts": {
    "build": "esbuild --bundle --minify --sourcemap --outdir=dist app=client/index.tsx style=client/style.css",
    "watch": "esbuild --bundle --watch --sourcemap --outdir=dist app=client/index.tsx style=client/style.css"
  },
  "devDependencies": {
    "@types/react": "^18.0.15",
    "@types/react-dom": "^18.0.6",
    "esbuild": "^0.14.49",
    "typescript": "^4.7.4"
  },
  "dependencies": {
    "normalize.css": "^8.0.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
